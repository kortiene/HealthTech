# Flutter / Dart
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
build/
# pubspec.lock IS committed: app-patient is an application (not a library), so a
# locked, reproducible dependency set is correct — and the CI SCA gate scans it
# (osv-scanner, ci.yml). Keep it tracked, consistent with Cargo.lock / package-lock.json.

# Generated bindings (FRB + drift) — regenerated via codegen.
# frb_generated.dart IS committed: codegen is a dev-time operation; CI must
# not depend on flutter_rust_bridge_codegen being installed (#102).
*.g.dart
*.freezed.dart

# IDE / OS
.idea/
*.iml
.DS_Store
