# osv-scanner suppression list — aligns with deny.toml ignore list.
# Advisories here are transitive deps with no direct usage in the crypto path.
# Each entry documents why the risk is acceptable and when to revisit.

[[IgnoredVulns]]
id = "RUSTSEC-2024-0436"
reason = "paste — unmaintained (no active exploit). Transitive dep via proc-macros (sqlx/thiserror). No fix available upstream; revisit on sqlx 0.8 migration."

[[IgnoredVulns]]
id = "RUSTSEC-2023-0071"
reason = "rsa 0.9.10 — timing side-channel in RSA decryption. HealthTech uses AES-256-GCM only; RSA is not in any code path. Transitive dep via sqlx-postgres."

[[IgnoredVulns]]
id = "RUSTSEC-2025-0134"
reason = "rustls-pemfile — unmaintained. Transitive dep via sqlx runtime-tokio-rustls. No direct usage; revisit on sqlx 0.8."

[[IgnoredVulns]]
id = "RUSTSEC-2026-0098"
reason = "rustls-webpki — TLS name-constraint bypass. Backend connects to local PostgreSQL over loopback (127.0.0.1); no TLS termination in Rust. Revisit on sqlx 0.8 / rustls-webpki 0.103."

[[IgnoredVulns]]
id = "RUSTSEC-2026-0099"
reason = "rustls-webpki — wildcard name-constraint bypass. Same rationale as RUSTSEC-2026-0098 (loopback DB connection, no TLS in Rust)."

[[IgnoredVulns]]
id = "RUSTSEC-2026-0104"
reason = "rustls-webpki — reachable panic in CRL parsing. Same rationale as RUSTSEC-2026-0098 (loopback DB connection, no CRL path exercised)."

[[IgnoredVulns]]
id = "RUSTSEC-2024-0363"
reason = "sqlx 0.7.4 — h2 integer overflow; fixed in sqlx 0.8.1. Upgrade deferred (breaking API changes); backend does not expose HTTP/2. Revisit in next sprint."

[[IgnoredVulns]]
id = "GHSA-frvp-7c67-39w9"
reason = "@hono/node-server 1.19.14 — fixed in 2.0.5 (major version bump). adw_sdlc dev tooling only; not part of the patient data pipeline. Upgrade blocked by semver constraint; bump separately."

[[IgnoredVulns]]
id = "GHSA-2v37-7h3g-55p8"
reason = "nanoid 3.3.17 — dev dependency only (build tooling in adw_sdlc and app-medecin). Not in the patient data pipeline or any runtime code path. Fix requires major version bump to 5.x/6.x (ESM-only, breaking API); upgrade tracked separately."

[[IgnoredVulns]]
id = "RUSTSEC-2026-0253"
reason = "lru 0.16.4 — transitive dep of aws-sdk-s3 (S3 media storage). aws-sdk-s3 v1.141.0 still pins lru <0.18; fix requires upstream AWS SDK update. lru is used only for S3 SDK internals (request caching), not in the AES-256-GCM crypto path. Revisit on next aws-sdk-s3 minor that lifts the lru constraint."
